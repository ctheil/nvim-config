require("theil.remap")
require("theil.set")



-- function SourceAfterDirectory()
--     local after_dir = vim.fm.stdpath("config") .. "/after"
--     local files = vim.fn.globpath(after_dir, '*', false, true)
--
--     for _, file in ipairs(files) do
--         vim.cmd('source ' .. file)
--     end
-- end
--
-- vim.api.nvim_create_autocmd("VimEnter", {callback = SourceAfterDirectory})


